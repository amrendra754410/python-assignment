#Create a program that checks if the temperature is either below 0°C (cold) or above 30°C (hot) and
# alerts the user accordingly. Use Boolean operators to determine the conditions.


def for_temp(num):
    if(num<0 or num):
        return